## macsocket client (PyInstaller)

### macOS icon + metadata

`client/macsocket-client.spec` is configured to build a macOS `.app` bundle and
expects an icon at `client/icon.icns`.

- Put your `.icns` file at `client/icon.icns`
- Rebuild with PyInstaller using the spec file

